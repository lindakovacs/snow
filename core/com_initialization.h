#pragma once

namespace Core
{
  class ComInitialization
  {
  public:
    ComInitialization();
    ~ComInitialization();

  private:
    ComInitialization(const ComInitialization&);
    ComInitialization& operator = (const ComInitialization&);
  };
}
